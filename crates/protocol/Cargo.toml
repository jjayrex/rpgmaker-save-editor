[package]
name = "rpgsave-protocol"
version = "0.2.0"
edition = "2024"
description = "Types exchanged between the Tauri backend and the Leptos front end"

[dependencies]
serde = { version = "1", features = ["derive"] }
